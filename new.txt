If it's installed but aws isn't recognized in CMD:

Press Win + S, search for Environment Variables, and open "Edit the system environment variables".

In System Properties, click Environment Variables.

Under System Variables, find Path, click Edit.

Click New, then paste the path to where aws.exe is (e.g. C:\Program Files\Amazon\AWSCLI\bin).

Click OK to apply and restart CMD.
